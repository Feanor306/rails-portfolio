<div class="col-md-8">
  <% if logged_in?(:site_admin) %>
    <div class="mb-3 mt-3">
      <%= link_to 'New Blog', new_blog_path, class: "btn btn-primary" %>
    </div>
  <% end %>

  <%= render @blogs %>

  <%= paginate @blogs %>
</div>