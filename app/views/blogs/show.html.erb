<div class="col-md-8">
  <h2>
    <%= @blog.title %>
  </h2>

  <%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin) %>

  <p id="notice"><%= notice %></p>

  <p>
    <%= @blog.body %>
  </p>

  <%= link_to 'Back', blogs_path %>
</div>